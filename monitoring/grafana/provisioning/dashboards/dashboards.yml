apiVersion: 1

providers:
  - name: webhook-service
    orgId: 1
    folder: Webhook Service
    folderUid: webhook-service
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
