/**
 * HTTP client for backend API
 * Provides methods for all API endpoints
 */

// TODO: Implement API methods:
// - fetchWebhooks(page, size)
// - createWebhook(data)
// - getWebhook(id)
// - updateWebhook(id, data)
// - deleteWebhook(id)
// - toggleWebhook(id)
// - fetchRequests(webhookId, page, size)
// - getRequest(webhookId, requestId)
// - clearRequests(webhookId)
// - getStats(webhookId)
// - previewTemplate(template, data)

const API = {
    baseUrl: '/api',
    // API methods will be implemented here
};
